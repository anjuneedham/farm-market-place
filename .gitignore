# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
next-env.d.ts

# production data (memory data source dev persistence)
.data/

# uploads (local storage service)
public/uploads/

# env
.env
.env.local
.env.*.local
!.env.example

# misc
.DS_Store
*.pem
coverage/
.vercel
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# locally-installed agent skills/plugins (not part of the application)
.agents/
.claude/
skills-lock.json

# Android (Capacitor) — build output and machine-local config regenerate via
# `npx cap sync android`; never commit signing secrets (see docs/PLAY_STORE_TESTING.md)
android/app/build/
android/build/
android/.gradle/
android/.idea/
android/local.properties
android/app/*.keystore
android/app/*.jks
android/keystore.properties
android/captures/
android/*.iml
android/app/*.iml
